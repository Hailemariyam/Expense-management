# Copy to .env.local

# Base URL of the Express backend (server-side calls from the Next.js BFF).
# The browser never talks to this directly — it goes through /bff/* route handlers.
BACKEND_URL=http://localhost:4000

# Marks the refresh-token cookie Secure. Leave false for local http.
COOKIE_SECURE=false
